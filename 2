

<!DOCTYPE html>
<html lang="en">
<head>
	<link rel="stylesheet" href="Style.css">
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Мой первый сайт</title>
</head>
<body>
	<h3>3.Объявить две целочисленные переменные a и b и задать им произвольные начальные значения. Затем написать скрипт, который работает по следующему принципу:
если a и b положительные, вывести их разность;
если а и b отрицательные, вывести их произведение;
если а и b разных знаков, вывести их сумму;
ноль можно считать положительным числом.</h3>
				<script>
				function twovariables (){
					var a = +prompt('Введите значение переменной a:');
					var b = +prompt('Введите значение переменной b:');
					var c;

					if(isNaN(a) || isNaN(b)) {
						alert('Значение должно быть числовым!');
						while (isNaN(a) || isNaN(b)) {
							a = +prompt('Введите числовое значение переменной a:');
							b = +prompt('Введите числовое значение переменной b:');
						}
					}
					if(a > 0 && b > 0) {
						var twoVariables = a - b;
						 alert ('Обе переменные имеют положительное значени , их разность '+ twoVariables );
					} else if( a < 0 && b < 0) {
						twoVariables = a * b;
						 alert ('Обе переменные имеют отрицательное значение  , их прозаедение' + twoVariables );
					} else if (a < 0 || b < 0) {
						twoVariables = a + b;
						alert ('Переменные имеют разные знаки сумма переменных' + twoVariables ); 
					} else {
						twoVariables = a - b;
						alert ('Одна или обе переменных имеют значение "0", выводится разность переменных' + twoVariables );
					}
					
				}
				 twovariables()
				</script>
 <h3>4. Присвоить переменной а значение в промежутке [0..15]. С помощью оператора switch
							организовать вывод чисел от a до 15.
</h3>

					<script>
						function random() {
							var a = +parseInt(Math.random() * (15 - 0) + 0);
							var row = 15 - a;
							var arr = [];
							var arrFull = []; 
							for (var i = 0; i <= row; i++) {
								arr.push(i);
								arrFull.push(arr[i] + a);
							}
							alert ( 'Переменная "а" приняла значение: ' + a + '.<br>Числовой ряд от "а" до 15 включает следующие числа: ' + arrFull);
						}

						random()
					</script>

 <h3>4. Присвоить переменной а значение в промежутке [0..15]. С помощью оператора switch
							организовать вывод чисел от a до 15.
</h3> 5. Реализовать основные 4 арифметические операции в виде функций с двумя параметрами. Обязательно использовать оператор return. 
				<script>
					function start(){
						var a = +prompt('Введите значение переменной "а": ');
						var b = +prompt('Введите значение переменной "b": ');
						var d = prompt('Введите арифметическое действие: ', '+, -, /, *');
						var t;
						if(d == '+') {
							function addition(a, b) {
								return a + b;
							}
							t = 'Арифметическое действие: Сложение.<br>Сумма переменных равна: ' + addition(a, b);
						} else if(d == '-') {
							function subtraction(a, b) {
								return a - b;
							}
							t = 'Арифметическое действие: Вычитание.<br>Разность переменных равна: ' + subtraction(a, b);
						} else if (d == '/') {
							function division(a, b) {
								return a / b;
							}
							t = 'Арифметическое действие: Деление.<br>Частное переменных равно: ' + division(a, b);
						} else if (d == '*') {
							function multiplication(a, b) {
								return a * b;
							}
							t = 'Арифметическое действие: Умножение.<br>Произведение переменных равно: ' + multiplication(a, b);
						} else {
							alert('Вы ввели неверный символ!');
							t = 'Арифметическое действие: Не указано пользователем.';
						}
						alert ('Значение переменной "а" равно: ' + a + '.<br>Значение переменной "b" равно: ' + b + '.<br>' + t + '.');
					}

					start()
				</script>
</body>
</html>								